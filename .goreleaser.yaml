version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: dbtui
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/alaa/dbtui/cmd.version={{.Version}}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats_overrides:
      - goos: windows
        formats:
          - zip

nfpms:
  - package_name: dbtui
    vendor: alaa
    homepage: https://github.com/alaash3lan/dbtui
    maintainer: alaa
    description: A modern terminal-based MySQL/MariaDB client
    license: MIT
    formats:
      - deb
      - rpm
      - apk
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
